server {
    listen 80;
    listen [::]:80;
    server_name _;

    # Minimal non-default server returning 404 to avoid duplicating the
    # default_server declared in 000-default.conf
    location / {
        return 404;
    }
}
